term=ghostty
window-padding-color = background
theme=TokyoNight Night
# theme = vesper
font-family = "Jet Brains Mono Nerd Font"
font-size = 14
background=000000
# background-opacity=0.85
background-blur = false
font-feature = -calt, -liga, -dlig

shell-integration-features = no-cursor,no-sudo,no-title
gtk-titlebar = false

mouse-scroll-multiplier = 0.3

# what is a prefix?
# keybind = ctrl+d=scroll_page_fractional:0.5
# keybind = ctrl+u=scroll_page_fractional:-0.5


# This is the configuration file for Ghostty.
#
# Run `ghostty +show-config --default --docs` to view a list of
# all available config options and their default values. Each
# config option is also explained in detail on Ghostty's website,
# see https://ghostty.org/docs/config.
#
# # Some config options have unique syntaxes for their value,
# # which is explained in the docs for that config option.
# # Just for example:
# resize-overlay-duration = 4s 200ms

